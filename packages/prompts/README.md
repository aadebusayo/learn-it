# Prompts

This package stores the durable agent constitution and specialized agent prompt rules. Runtime code should compose these rules with structured learner state and graph evidence rather than relying on free-form prompt text alone.
